specVersion: "v1"
serviceId: "fuel-station-app"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "fuel-station-app-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
